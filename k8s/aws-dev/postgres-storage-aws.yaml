apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgdata-pvc
  namespace: atoloan-postgres-dev
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path   # k3s default storage class
  resources:
    requests:
      storage: 3Gi
